{{- if .Values.api.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mimisee.fullname" . }}
  labels:
    {{- include "mimisee.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "mimisee.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
{{- end }}
