{{- if .Values.grafana.dashboard.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mimisee.fullname" . }}-grafana-dashboard
  labels:
    {{- include "mimisee.labels" . | nindent 4 }}
    app.kubernetes.io/component: observability
    {{- with .Values.grafana.dashboard.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.grafana.dashboard.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
data:
  grafana-dashboard-mimisee.json: |-
{{ .Files.Get "files/grafana-dashboard-mimisee.json" | indent 4 }}
{{- end }}
